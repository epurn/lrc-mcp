# Environment configuration for lrc-mcp

# HTTP server port (default: 8765)
LRC_MCP_HTTP_PORT=8765

# Plugin authentication token (optional, but recommended for production)
# If set, the Lightroom plugin must send the same token in X-Plugin-Token header
# LRC_MCP_PLUGIN_TOKEN=your-secret-token-here

# Optional override for Lightroom Classic executable path
# LRCLASSIC_PATH=C:\Program Files\Adobe\Adobe Lightroom Classic\Lightroom.exe
